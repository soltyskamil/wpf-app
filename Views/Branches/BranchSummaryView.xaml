<UserControl
    x:Class="MVVMGym.Views.BranchSummaryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MVVMGym.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="LabelTextStyle" TargetType="TextBlock">

                <Setter Property="FontWeight" Value="DemiBold" />
                <Setter Property="FontSize" Value="16" />


            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DataGrid
            AutoGenerateColumns="False"
            IsReadOnly="True"
            ItemsSource="{Binding BranchesOverview}"
            RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding name}" Header="Name" />
                <DataGridTextColumn Binding="{Binding address}" Header="address" />
                <DataGridTextColumn Binding="{Binding phone}" Header="phone" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding openTime}"
                    Header="open time" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding closeTime}"
                    Header="close time" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding createdAt}"
                    Header="createdAt" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding updatedAt}"
                    Header="updatedAt" />

            </DataGrid.Columns>


            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <DataGrid
                        Padding="20"
                        AutoGenerateColumns="False"
                        ColumnWidth="150"
                        IsReadOnly="True"
                        ItemsSource="{Binding overviews}">

                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding name}" Header="Name" />
                            <DataGridTextColumn Binding="{Binding count}" Header="Total Count" />
                            <DataGridTextColumn Binding="{Binding totalValue}" Header="Total Value" />
                            <DataGridTextColumn Binding="{Binding average}" Header="Average Value" />
                        </DataGrid.Columns>
                    </DataGrid>

                </DataTemplate>

            </DataGrid.RowDetailsTemplate>
        </DataGrid>

    </Grid>
</UserControl>
