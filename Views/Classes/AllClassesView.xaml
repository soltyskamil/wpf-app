<UserControl
    x:Class="MVVMGym.Views.AllClassesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MVVMGym.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DataGrid
            Grid.Row="0"
            Grid.Column="0"
            AutoGenerateColumns="False"
            ItemsSource="{Binding ClassesList}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    x:Name="class_id"
                    Binding="{Binding Path=class_id}"
                    Header="id" />
                <DataGridTextColumn
                    x:Name="name"
                    Binding="{Binding Path=name}"
                    Header="Nazwa" />
                <DataGridTextColumn
                    x:Name="duration"
                    Binding="{Binding Path=durationMinutes}"
                    Header="Czas trwania (min)" />
                <DataGridTextColumn
                    x:Name="difficulty"
                    Binding="{Binding Path=difficulty}"
                    Header="Poziom trudnosci" />
                <DataGridTextColumn
                    x:Name="status"
                    Binding="{Binding Path=status}"
                    Header="Status" />
                <DataGridTextColumn
                    x:Name="branchName"
                    Binding="{Binding Path=branchName}"
                    Header="Nazwa siłowni" />
                <DataGridTextColumn
                    x:Name="trainerName"
                    Binding="{Binding Path=trainerName}"
                    Header="Imie trenera" />
                <DataGridTextColumn
                    x:Name="created"
                    Binding="{Binding Path=createdAt}"
                    Header="Dodano" />
                <DataGridTextColumn
                    x:Name="updated"
                    Binding="{Binding Path=updatedAt}"
                    Header="Zmodyfikowano" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
