<UserControl x:Class="MVVMGym.Views.MembershipPlansView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMGym.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid  AutoGenerateColumns="False"  ItemsSource="{Binding MembershipList}">
            <DataGrid.Columns>
                <DataGridTextColumn  x:Name="plan_id"  Binding="{Binding  Path=plan_id}" Header="id" />
                <DataGridTextColumn  x:Name="name"  Binding="{Binding  Path=name}" Header="Nazwa"/>
                <DataGridTextColumn  x:Name="durationMonths"  Binding="{Binding  Path=durationMonths}" Header="Czas trwania"/>
                <DataGridTextColumn  x:Name="cena"  Binding="{Binding Path=price}"  Header="cena"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
