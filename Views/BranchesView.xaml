<UserControl
    x:Class="MVVMGym.Views.BranchesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MVVMGym.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />

        </Grid.ColumnDefinitions>

        <DataGrid
            Grid.Row="0"
            Grid.Column="0"
            AutoGenerateColumns="False"
            ItemsSource="{Binding BranchesList}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    x:Name="branch_id"
                    Binding="{Binding Path=branch_id}"
                    Header="id" />
                <DataGridTextColumn
                    x:Name="name"
                    Binding="{Binding Path=name}"
                    Header="Nazwa" />
                <DataGridTextColumn
                    x:Name="address"
                    Binding="{Binding Path=address}"
                    Header="Adres" />
                <DataGridTextColumn
                    x:Name="phone"
                    Binding="{Binding Path=phone}"
                    Header="Numer tel" />
                <DataGridTextColumn
                    x:Name="openTime"
                    Binding="{Binding Path=openTime}"
                    Header="Czas otwarcia" />
                <DataGridTextColumn
                    x:Name="closeTime"
                    Binding="{Binding Path=closeTime}"
                    Header="closeTime" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
